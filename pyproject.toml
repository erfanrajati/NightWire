[project]
name = "nightwire"
version = "1.0.2"
description = "Local-network files, protected clipboard sharing, lifecycle controls, and client visibility"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "qrcode>=8.2,<9.0",
    "starlette>=0.48,<1.0",
    "uvicorn>=0.35,<1.0",
]

[tool.uv]
package = false
